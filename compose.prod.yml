services:
  datadog-agent:
    image: gcr.io/datadoghq/agent:7
    restart: unless-stopped
    environment:
      - DD_API_KEY=${DD_API_KEY}
      - DD_SITE=${DD_SITE:-datadoghq.com}
      - DD_DOGSTATSD_NON_LOCAL_TRAFFIC=true
      - DD_HOSTNAME=rail-archiver
    expose:
      - "8125/udp"

  app:
    image: ghcr.io/ankoure/us-rail-performance-archiver:${IMAGE_TAG:-latest}
    restart: unless-stopped
    user: "1000:1000"
    # The image's HEALTHCHECK marks the container unhealthy if the poll loop
    # stops refreshing poll_state/.heartbeat. `restart: unless-stopped` does not
    # react to health status, so the autoheal sidecar (below) watches for the
    # `autoheal=true` label and restarts unhealthy containers.
    labels:
      - autoheal=true
    volumes:
      - ./config:/app/config:ro
      - ./archive:/app/archive
      - ./poll_state:/app/poll_state
    env_file:
      - .env
    depends_on:
      - datadog-agent

  autoheal:
    image: willfarrell/autoheal:latest
    restart: unless-stopped
    environment:
      - AUTOHEAL_CONTAINER_LABEL=autoheal
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

  batch:
    image: ghcr.io/ankoure/us-rail-performance-archiver:${IMAGE_TAG:-latest}
    restart: unless-stopped
    user: "1000:1000"
    # The image HEALTHCHECK probes poll_state/.heartbeat (the poller's liveness file),
    # which this rollup+ship loop never writes, so it would be perpetually "unhealthy".
    # It carries no autoheal label, so nothing acts on it — disable the inherited check
    # rather than report a misleading status.
    healthcheck:
      disable: true
    environment:
    - PYTHONUNBUFFERED=1
    - ROLLUP_WORKERS=${ROLLUP_WORKERS:-4}
    volumes:
      - ./config:/app/config:ro
      - ./archive:/app/archive
      - ./curated:/app/curated
    env_file:
      - .env
    depends_on:
      - datadog-agent
    command: sh -c "while true; do DAY=$$(date -u -d yesterday +%F); python rollup.py --day $$DAY && python ship.py --day $$DAY; sleep 86400; done"
